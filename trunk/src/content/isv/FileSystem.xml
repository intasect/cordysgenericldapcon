
<filesystem>
	<file
		source="${lib.dir}/coelib.jar"
		destdir="${project.deploy}"
		ecxdir="cordys_install_dir"
	/>
	<file
		source="${build.web}"
		destdir="\Web${project.deploy}"
		ecxdir="cordys_install_dir"
	/>
	<file
		source="${build.jar}/${project.jar.name}.jar"
		destdir="${project.deploy}"
		ecxdir="cordys_install_dir"
	/>
	<file
		source="${build.apidocs}"
		destdir="${project.deploy}\docs\javadoc"
		ecxdir="cordys_install_dir"
	/>
</filesystem>
